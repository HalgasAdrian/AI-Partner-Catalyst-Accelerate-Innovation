// Root component for the front‑end application.
//
// Use this component to build the chat or code‑prompt interface.  A basic
// implementation might consist of:
//   * A text area or editor for the user to enter the prompt and optional test cases.
//   * A button to submit the prompt to the backend API.
//   * A display area showing the generated code, execution output and error
//     messages for each iteration.
//   * Loading indicators and error handling UI.
//
// Use a modern JavaScript framework (React, Vue, etc.) to manage state and
// asynchronous API calls.  The implementation details are omitted here.

function App() {
  return (
    <div className="app-container">
      <h1>Self‑Debugging Coding Assistant</h1>
      {/* TODO: Implement the interactive UI here */}
    </div>
  );
}

export default App;